{
  "name": "password-based-auth",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon --env-file=.env server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "montyaction",
  "license": "ISC",
  "description": "A simple web-based authentication system demonstrating core password hashing and verification principles. Users can register with a username and password, and log in to a protected area.",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "dotenv": "^16.4.7",
    "mongodb": "^6.14.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
